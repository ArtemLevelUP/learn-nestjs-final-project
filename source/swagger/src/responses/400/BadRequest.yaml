description: 'Не верный пейлоад'
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          example: 'Bad Request'
        message:
          type: array
          items:
            type: string
            example: [
              "sex must be a valid enum value",
              "role must be a valid enum value"
            ]
