description: 'Получен пользователь с помощью hash'
content:
  application/json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: './_User.yaml'
